# DECISIONS.md — قرارات الغموض

كل قرار هنا اتُّخذ بمبدأ «الأقرب لسلوك `anaqatuk.html`»، ويُسجَّل هنا كما نصّ البرومبت. تُحدَّث القائمة كلما ظهر غموض جديد أثناء التنفيذ.

| # | الموضوع | الغموض | القرار | المبرر |
|---|---|---|---|---|
| D-01 | الخط الأساسي | البرومبت يذكر **Tajawal**، النموذج يحمّل **IBM Plex Sans Arabic** | نعتمد **IBM Plex Sans Arabic** للنصوص و**El Messiri** للعناوين، مع إبقاء Tajawal بديلًا احتياطيًا في الثيم | النموذج مصدر الحقيقة؛ نوثّق التعارض فقط |
| D-02 | نوع الحساب | هل يوجد حساب «مشترية» و«بائعة»؟ | حساب واحد **ببروفايل مزدوج** ومحفظة موحدة؛ البيع يتفعّل بإكمال ملف المتجر + التوثيق | §٢ + شاشة `buyerDash` |
| D-03 | أساس احتساب العمولة | العمولة على السعر قبل أم بعد الكوبون؟ | **بعد كوبون البائعة**؛ كوبون المنصة على حساب المنصة ولا يمس مستحقات البائعة | نص لوحة الكوبونات: «العمولة تُحسب على السعر بعد الخصم» |
| D-04 | كود التسليم/OTP في التطوير | القيمة | `1111` في بيئة التطوير فقط خلف `SmsChannel`/`FakeGateway`؛ يُعطَّل في الإنتاج | تعليقات JS «جرّبي 1111» |
| D-05 | حجب قطعة التأجير | نطاق الحجب | إيجار + فترة إرجاع/ترانزيت + يوم فحص واحد؛ لا تتاح إلا بتأكيد البائعة «سليمة» | جدول التوفر ونص لوحة المتجر |
| D-06 | تعدد البائعات في الطلب | كيف يُقسَّم؟ | طلب أب + طلبات فرعية لكل بائعة، **شحنة وكود تسليم وEscrow مستقل** لكل واحدة (مثال A-1043 و A-1043-B) | ملاحظة السلة وصفحة الطلبات |
| D-07 | رفض عرض الطلب الذكي | سلوك التجاوز | العرض > الميزانية **يُرفض خادميًا** ولا يظهر للعميلة (لا يُخزَّن كعرض ظاهر) | `srBid`: «ما راح يظهر للعميلة» |
| D-08 | «الدفع أولًا» | متى يُفتح الشات؟ | الشات مقفل حتى `paid_escrow`، ثم يُفتح تلقائيًا مع تنبيه للمشترية قبل الدفع | §٤.٨ وسياسة المتجر |
| D-09 | الأرقام العربية-الهندية | تخزين أم عرض؟ | تُخزَّن لاتينية (`DECIMAL`)، وتُعرَض عربية-هندية عبر مُنسّق (`arNum`) في الطبقة الأمامية | §٠ |
| D-10 | فلتر منع التعامل الخارجي | النموذج يذكره نصًّا فقط | نُنفّذه فعليًا: كشف أرقام هواتف/روابط/كلمات دفع خارجية في `messages`، استبدال/تحذير + إشعار أدمن (بلاغ) | §٤.١٠ والملاحظة النظامية بالشات |
| D-11 | مؤشر ثقة البائعة | كيف يُحتسب؟ | مشتق آليًا (متوسط التقييمات + نسبة الالتزام بالمواعيد + عدد الصفقات الموثقة)، للقراءة فقط، غير قابل للتعديل اليدوي | بطاقات العروض + سياسة التقييم |
| D-12 | AI التعبئة التلقائية | مصدر البيانات | `AiListingService` بواجهة ثابتة + Mock يعيد نتيجة النموذج الحرفية؛ endpoint خلفي يستقبل الصورة، جاهز للربط بـVision API | §نموذج النشر + `aiFill` |
| D-13 | المبالغ | نوع الحقل | `DECIMAL(10,2)` في كل مكان، ممنوع Float؛ التقريب `round(x*100)/100` كما في JS | §٦ + `confirmDelivery` |
| D-14 | التقويم في رسم المبيعات | هجري أم ميلادي؟ | أشهر هجرية في العرض التوضيحي؛ التخزين بتواريخ ميلادية | رسم لوحة المتجر (محرم…جمادى٢) |
| D-15 | حد الطلبات الذكية النشطة | القيمة | ٣ طلبات نشطة/مستخدمة (من البرومبت §٤.٧)؛ النموذج لا يقيّد صراحةً — نعتمد البرومبت | البرومبت أكثر تحديدًا ولا يتعارض مع النموذج |
| D-16 | إصدار PHP | البرومبت طلب 8.3، لكن اللوك حُلّ على 8.4 | نعتمد **PHP 8.4** (طوّرنا واختبرنا عليه؛ مكوّنات Symfony 8.1 المقفلة تتطلب `php ≥ 8.4.1`) في التطوير والـCI | بيئة الساندبوكس وفّرت 8.4 وحلّت التبعيات عليه |
| D-17 | توقيع Android | لا يمكنني ضبط أسرار المستودع، والمستخدم يحتاج APK موقّعًا قابلًا للتثبيت مباشرة | نُودِع **keystore تجريبي موقّع ذاتيًا** في المستودع (استثناء واضح في `.gitignore`) ليوقّع الـCI بثبات (v1+v2+v3) وينتج APK قابلًا للتثبيت على الأجهزة الصارمة (OnePlus). **يجب استبداله بمفتاح مُدار بالأسرار قبل نشر Play Store.** بديل الإنتاج: أسرار `ANDROID_KEYSTORE_BASE64` + `ANDROID_KEY_PROPERTIES` تتجاوزه تلقائيًا | «App not installed» سببه توقيع debug الاحتياطي؛ التوقيع الفعلي بمفتاح release يحلّه |
