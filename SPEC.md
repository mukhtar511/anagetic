# SPEC.md — أناقتك (Anaqatuk)

> **مصدر الحقيقة الأوحد:** `anaqatuk.html` (٣٬٢٩٣ سطرًا). هذا المستند استخراج كامل للشاشات، قواعد العمل، النصوص العربية الحرفية، آلات الحالة، والبيانات التجريبية — ليكون العقد المرجعي لبناء الباك إند (Laravel 11) والأدمن (Filament 3) وتطبيق Flutter.
>
> **حالة المرحلة ٠:** جاهز للمراجعة والتأكيد قبل بدء المرحلة ١.

---

## ٠) هوية المشروع

| المفتاح | القيمة |
|---|---|
| الاسم | أناقتك — ANAQATUK |
| الوصف | سوق سعودي متعدد البائعات لأزياء المصممات: **بيع**، **بيع بتعديل مقاسات**، **تأجير**، بالإضافة لمنتجات العناية والإكسسوارات والمستعمل الموثّق وبدون براند |
| اللغة | عربي أولًا، RTL بالكامل. أرقام عربية-هندية في العرض (١٢٣) وتخزين لاتيني في القاعدة |
| العملة | ريال سعودي (ر.س) — كل المبالغ `DECIMAL(10,2)`، ممنوع Float |
| المنطقة الزمنية | Asia/Riyadh |
| التقويم المعروض للبائعة | أشهر هجرية في الرسوم (محرم، صفر، ربيع١…) وميلادية في التواريخ التشغيلية |

### الهوية البصرية (من متغيرات CSS في `:root`)

| المتغير | اللون | الاستخدام |
|---|---|---|
| `--maroon` | `#6D28A9` | بنفسجي ملكي غني — الأساسي |
| `--maroon-deep` | `#4A1878` | بنفسجي عميق — الهيدر/الفوتر/العناوين |
| `--rose-bg` | `#F6EEFC` | لافندر فاتح — خلفية الصفحة |
| `--rose-soft` | `#E3CDF5` | لافندر — الحدود |
| `--gold` | `#B98A4E` | ذهبي — التأجير والأزرار الثانوية |
| `--gold-light` | `#D9B98A` | ذهبي فاتح |
| `--ink` | `#2E2438` | لون النص |
| `--white` | `#FFFEFF` | البطاقات |
| `--green` | `#4A6B4F` | التوثيق/النجاح |
| `--radius-arch` | `18px` | زوايا القوس المعمارية |

- **الخطوط:** العناوين `El Messiri` (400–700)، النصوص `IBM Plex Sans Arabic` (300–700). *ملاحظة:* البرومبت يذكر Tajawal — النموذج الفعلي يستخدم IBM Plex Sans Arabic؛ نعتمد النموذج ونوثّق ذلك في DECISIONS.
- **تدرّجات مميزة:** أقسام الفئات `c1..c6`، صور المنتجات `pi1..pi8` (كل واحدة تدرّج لوني ثابت — نحفظها كـ theme tokens).
- **تفاصيل معمارية:** بطاقات الأقسام بنصف قوس (`border-radius:120px 120px 14px 14px`)، حرف «أ» ذهبي شفاف خلف الهيرو، زخارف `✦`.
- **حركة:** احترام `prefers-reduced-motion` (إيقاف كل transitions/animations).

---

## ١) قرارات التقنية (نهائية)

| الطبقة | القرار |
|---|---|
| Flutter | أحدث Stable · Riverpod · go_router · dio · flutter_secure_storage · دعم RTL · Google Fonts (El Messiri + IBM Plex Sans Arabic) |
| Backend | Laravel 11 · PHP 8.3 · MySQL 8 · Sanctum (توكن) · Redis (طوابير/كاش) · Laravel Echo + Soketi (Pusher protocol) للحيّ |
| Admin | Filament 3 على نفس مشروع Laravel |
| ملفات | S3-متوافق (local driver في التطوير) |
| إشعارات | FCM خلف تجريد `NotificationChannel` (SMS لاحقًا) |
| دفع | تجريد `PaymentGateway` (authorize/capture/refund/payout) + `FakeGateway` كامل الوظائف. لا ربط منطق بمزود بعينه |
| OTP | تجريد `SmsChannel` + Driver وهمي يقبل `1111` في التطوير فقط |
| AI | تجريد `AiListingService` بواجهة ثابتة + Mock داخلي (جاهز لـ Vision API) |
| التوثيق | Scribe لتوثيق REST API |

---

## ٢) الأدوار

1. **زائرة (Guest):** تصفح كل العام (الرئيسية، المنتجات، المصممات، السياسات، المميز). أي شاشة شخصية → بوابة OTP.
2. **مستخدمة (بروفايل مزدوج):** كل حساب **مشترية وبائعة معًا** — لا يوجد نوعا حساب. البيع يتفعّل بإكمال «ملف المتجر» + التوثيق. محفظة **موحدة** واحدة.
3. **أدمن المنصة:** عبر Filament فقط.

### بوابة المصادقة المركزية (`GATED`)
الشاشات التالية تتطلب تسجيل — الوصول بدون تسجيل يفتح مودال OTP ويخزّن `pendingView`/`pendingAction` ثم يكمل للوجهة بعد النجاح:
`ordersPage, chatPage, favsPage, listingPage, sellerPage, buyerDash, smartReqPage`.
- عند نية النشر بدون تسجيل: `⚠ ما تقدرين تعلنين عن أي منتج قبل التسجيل — أدخلي رقم جوالك وبنرسل لك كود التحقق`.

---

## ٣) الشاشات الـ١٦ (مطابقة للنموذج)

> النصوص العربية أدناه **حرفية** من النموذج. عند البناء تُنقل كما هي.

### ٣.١ الرئيسية (`mainSite`)

**الشريط العلوي:** `🚚 شحن مجاني للطلبات فوق ٣٠٠ ر.س — واستأجري فستان المناسبة بدل ما تشترينه ✨`

**الهيدر:** شعار `أناقتك / ANAQATUK` · بحث (placeholder `ابحثي عن عباية، فستان، مصممة...`، زر `بحث`) · زر `＋ أضيفي منتج` · محدد المنطقة (١٢ منطقة، افتراضي الرياض) · أيقونات: `🔔 إشعاراتي` بعداد `nBadge`، `♡ المفضلة`، `👜 السلة`، `👤 حسابي`.

**شريط الأقسام (`mainnav`):** رقائق الفئات: `🖤 عبايات` (abaya) · `👗 فساتين` (dress) · `🌿 أدوات عناية` (care) · `💍 إكسسوارات` (acc) · `📦 بدون براند` (nobrand) · `♻️ المستعمل` (used) — ثم روابط: `⭐ المميزة` · `🪄 الطلب الذكي` · `المصممات` · `✦ التأجير` · `طلباتي` · `المحادثات 💬`. يلتف بالجوال.

**الهيرو:** eyebrow `سوق المصممات السعوديات` · عنوان `كل مصممة لها متجرها.. وأنتِ لكِ أناقتك` · وصف `تسوّقي عبايات وفساتين وخلطات عناية وإكسسوارات من مصممات موثّقات — واشترِي القطعة أو استأجريها لمناسبتك.` · مبدّل `شراء`/`تأجير` (buy↔sale, rent) يفلتر ويمرّر لقسم المنتجات · أزرار `تسوّقي الآن`/`انضمي كمصممة` · إحصائيات: `+١٢٠ مصممة موثّقة`، `🔒 دفع آمن بكود تسليم`، `↩️ مجاني استرجاع ٧ أيام`، `👗 اشترِي أو استأجري`.

**بانر الطلب الذكي:** `ما تبين تدورين؟ اكتبي طلبك — والبائعات يتنافسن عليه` + مثال + زر `أنشئي طلبك الذكي ←`.

**شريط الإعلانات المميزة (`featStrip`):**
- تمرير تلقائي (`setInterval 30ms`, RTL scrollLeft سالب، يلتف عند الحافة) **يتوقف** بـ `mouseenter`/`touchstart` (`featPause=true`).
- فلترة بالنطاق: بطاقة تظهر إذا `scope==='all'` أو `scope===userRegion`.
- شارات: `🌍 كل المناطق` / `📍 {المنطقة}` + `⭐ مميز`.
- بطاقة CTA أخيرة: `⭐ ميّزي إعلانك — يظهر هنا بأعلى الصفحة لكل الزائرات — من لوحة متجرك`.

**بانر التأجير:** `ليش تشترينه وتلبسينه مرة؟ استأجريه بجزء من سعره` + `تأمين مسترد بالكامل · توصلك معقّمة ومكوية · الإرجاع مجاني من بابك` → يفتح سياسة التأجير.

**شريط المصممات (`dStrip`):** بطاقات متجر أفقية (تقييم، طلبات مكتملة، سنة الانضمام، شارة `✓ مصممة موثّقة`). بطاقات موقوفة تعرض `⏸ لا تستقبل طلبات حاليًا`، ومقفلة `🔒 المتجر مقفل مؤقتًا` (grayscale).

**شريط التشكيلات (`products` + `pgrid`):**
- شريط فلاتر متقدم: القسم · المنطقة · المصممة/البائعة · السعر من/إلى · الترتيب (`الأحدث`/`السعر من الأقل`/`من الأعلى`/`الأعلى تقييمًا`) · زر `تطبيق`.
- رقائق فلترة سريعة: `الكل`/`للبيع`/`للتأجير`/`المستعمل`/`بدون براند`.
- ترتيب «القريبة منك أولًا»: `orderByRegion()` يرتّب البطاقات بحيث `data-region===userRegion` أولًا، ويضيف شارة خضراء `📍 قريبة منك — {المنطقة}`.
- لا نتائج: `ما فيه منتجات تطابق الفلتر — جرّبي توسيع نطاق السعر أو المنطقة.`

**قسم الثقة (٤ عناصر):** دفع آمن · استرجاع مجاني · مصممات موثّقات · فحص المستعمل (النصوص الحرفية في §الثقة أدناه).

**بانر الانضمام:** `عندك شغل يدوي أو تصاميم؟ افتحي متجرك في أناقتك` + `عمولة ١٢٪ على البيع الفعلي فقط، بدون رسوم اشتراك` + زر `جرّبي لوحة المصممة`.

**الفوتر:** أقسام (الأقسام / أناقتك / السياسات) + تواصل `📧 hello@anaqatuk.sa · 📱 920000000` + `© ٢٠٢٦ أناقتك — منصة سعودية موثّقة · سجل تجاري ٧٠٤XXXXXXX · موثّق في «معروف» ✓` + `وسائل الدفع: مدى · فيزا/ماستركارد · Apple Pay · تابي · تمارا`.

### ٣.٢ صفحة المنتج (`productPage`)

- Marquee: `✦ الدفع محفوظ لدى أناقتك حتى تأكيد الاستلام بكود التسليم · الاسترجاع خلال ٧ أيام مجاني — حق مكفول لك ✦`
- ترويسة: كود المنتج (`A2605 · مجموعة عيد ٢٠٢٦`) · `{المصممة} — موثّقة ✓` · العنوان · التقييم `★ ٤.٨ (٥٤ تقييم)`.
- **الأنماط الثلاثة (`buy-mode`)** ديناميكية من `MODES` للمنتج:
  - `ready` — «شراء بدون تعديلات». label السعر `السعر الأساسي`.
  - `custom` — «شراء بتعديل المقاسات». label `السعر الأساسي (شامل التعديل)`. لوحة `مقاساتك بالتفصيل`: مبدّل وحدة `سم`/`إنش` + ٧ حقول إلزامية: **الكتف، محيط الصدر، محيط الخصر، محيط الورك، محيط العضد، طول الكم، الطول الكامل** + `📎 إرفاق صورة المقاسات` + `📏 عرض دليل القياسات ←` + `ملاحظاتك للمصممة` (placeholder `مثال: أبي فتحة الكم أوسع شوي، والطول يلامس الأرض مع كعب ٧ سم...`).
  - `rent` — «تأجير». basePrice=rent.price، deposit=rent.dep. لوحة `📅 حجز التأجير`: `تاريخ المناسبة *` + `المقاس *`.
    - خطأ تاريخ: `✕ غير متاح — القطعة محجوزة أو في فترة الإرجاع والفحص. أقرب تاريخ متاح: ١٤ يوليو`
    - نجاح تاريخ: `✓ التاريخ متاح — ستُحجب القطعة تلقائيًا: أيام الإيجار + الإرجاع + يوم الفحص`
    - جدول توفر: `٨ – ١٠ يوليو` = `مؤجرة (حجز قائم)` · `١١ – ١٢ يوليو` = `في طريق العودة للمصممة 🚚` · `١٣ يوليو` = `فحص وتنظيف وتعقيم` · `من ١٤ يوليو` = `✓ متاحة للحجز`
    - تنبيه التأمين: `🛡️ التأمين الشامل منفصل ويُسترد كاملًا لمحفظتك بعد الإرجاع السليم — التلف الكامل = خصم التأمين بالكامل للمصممة، والتأخير ١٠٪ عن كل يوم.` + `🚚 الإرجاع مجاني ✓ — المندوب يستلمها من بابك بدون أي رسوم عليك. ممنوع الغسيل الذاتي، التنظيف على المصممة.`
- **الألوان بكميات مخفية:** `🎨 اختاري اللون` / `الألوان غير المتوفرة تظهر معطلة`. نفاد اللون = تعطيله فقط (`.out`)، الكمية لا تظهر إطلاقًا. تبديل اللون يغيّر تدرّج الصورة ويعيد الكمية إلى ١. عدّاد كمية `chgQty` مقيّد بـ `stock[curColor]`.
- **إضافات البائعة المسعّرة (`renderSellerAdds`):** checkbox فوري `{الاسم} +{السعر} ر.س` يضيف/يحذف سطرًا من الملخص. فارغ: `ما حددت المصممة إضافات جاهزة لهذا المنتج — اطلبي اللي تبين بالأسفل.`
- **طلب إضافة مخصصة:** input `مثال: فتحة ظهر مختلفة` + زر `إرسال للمصممة` + رقائق سريعة (`أكمام أطول`، `فتحة ظهر مختلفة`، `تغيير لون التطريز`). يُسعَّر بعرض رسمي في الشات (Mock: سعر عشوائي من `[80,120,150,200,250]`، تأخير ~١٨٠٠ms، نص `«أقدر أنفذها، وتزيد مدة التنفيذ يومين»`).
- **التغليف (`🎀 التغليف — حسب المتاح لدى البائعة`):** حتى ٤ مستويات حسب المتاح: `تغليف عادي مجاني` · `تغليف بسيط +١٥` · `تغليف سبيشل +٤٥` · `تغليف هدايا` (قد يكون `غير متاح لدى هذه البائعة`، غير قابل للاختيار).
- **ملخص السعر الحي (`updateTotal`):** `basePrice + additionsTotal + packPrice + deposit`. صفوف: `السعر الأساسي` · `التأمين الشامل المسترد 🛡️` (للتأجير) · `الإجمالي` · زر `👜 أضيفي للسلة` · note `الدفع الآجل متاح عبر تابي وتمارا · المبلغ محفوظ لدى أناقتك حتى الاستلام · الاسترجاع مجاني خلال ٧ أيام`.
- **صف التواصل حسب سياسة البائعة (`comm`):**
  - `chat` → دردشة فقط عبر المنصة (الرقم لا يظهر).
  - `call` → دردشة + اتصال، يظهر الرقم (`tel:` — مثال `0551234567`).
  - `payfirst` → `🔒 هذه البائعة تفتح المحادثة بعد إتمام الدفع فقط` مع صندوق تنبيه واضح قبل الدفع.
- **التقييمات:** `تقييمات العميلات (٥٤)` — بطاقات بشارة `✓ شراء موثّق` أو `✓ تأجير موثّق`، نجوم، نص. (٣ تقييمات تجريبية لـ dress1 — انظر §البيانات).
- Powered: `هذا المنتج ضمن متجر ريم كوتور الموثّق في منصة أناقتك`.

### ٣.٣ صفحة المصممة (`designerPage`)

Marquee `✦ قطع محدودة — التنفيذ حسب أولوية الطلب ✦ خصم ١٠٪ على أول طلب بكود: نورة١٠ ✦` · هيرو (أفاتار، اسم، `عبايات مناسبات · تفصيل حسب المقاس · الرياض`، bio) · أزرار `تسوّقي التشكيلة`/`💬 راسلي المصممة` · ٤ إحصائيات كل بشارة `✓ موثّق من أناقتك` (متوسط التقييم، طلب مكتمل، التزام بمدة التنفيذ، سنة الانضمام) · رقائق مجموعات (`مجموعة المناسبات`/`اليومي الفاخر`/`تفصيل خاص`/`قطع التأجير`) · شبكة منتجات · FAQ (٤ أسئلة حرفية) · powered + فوتر متجر.

### ٣.٤ نموذج النشر (`listingPage`)

- ترويسة `متجر: دار نورة للعبايات ✓` + شريط وضع التعديل (`✏️ وضع التعديل: التعديلات تنعكس على الإعلان المنشور فور الحفظ` + `إلغاء التعديل`).
- `📸 صور المنتج` (حد أقصى ٨) + **زر `✨ تعبئة تلقائية من الصورة (ذكاء اصطناعي)`** (`aiFill`: يعيد عنوان/وصف/لون/فئة/نطاق سعر مقترح — عبر `AiListingService`). خانات: `الصورة الرئيسية *` · صورة · صورة · فيديو قصير.
- `📝 بيانات المنتج`: `اسم المنتج *` · `القسم *` (فساتين/عبايات/أدوات عناية/إكسسوارات/بدون براند/المستعمل) · `حالة المنتج *` (جديد / مستعمل حالة ممتازة / جيدة جدًا / جيدة) · وصف.
- `🎨 الألوان والكميات`: hint `الكميات تظهر لك فقط ولا تظهر للعميلات إطلاقًا` · صفوف لون (اسم/صورة/كمية/حذف) + `+ أضيفي لون`.
- `🏷️ خيارات البيع والتأجير` (فعّلي ≥١): opt1 بيع بدون تعديل (`سعر البيع *` + مدة التجهيز) · opt2 بيع بتعديل (`سعر البيع مع التعديل *` + `مدة تنفيذ التعديل *` + note المقاسات السبعة) · opt3 تأجير (`سعر التأجير / ٣ أيام *` + `التأمين الشامل *` + نطاق + checkbox `أوافق على سياسة التأجير وتوثيق حالة القطعة بالصور`). تحذير: `⚠ لازم تفعّلين خيار واحد على الأقل`.
- `⭐ نوع الإعلان`: `إعلان عادي مجاني تمامًا` أو `⭐ إعلان مميز` (نطاق `📍 منطقتي فقط`/`🌍 جميع المناطق` × مدة `٣/٧/١٥` — مصفوفة الأسعار §٤) — يُدفع عند النشر من المحفظة أو بطاقة، انتهاء تلقائي.
- `✦ إضافات المنتج وأسعارها` (اختياري).
- `🎀 التغليف` (محفوظ من المتجر، يؤكَّد/يعدّل لهذا الإعلان).
- `📞 التواصل لهذا الإعلان` (الأوضاع الثلاثة، محفوظ من المتجر).
- `📍 المدينة والفروع` (افتراضية / مدينة أخرى / عدة فروع بسعري توصيل).
- **`↩️ سياسة الاسترجاع`** + checkbox إلزامي `أُقر بسياسة الاسترجاع المجاني وأني راعيت تكلفته في تسعيري *` (تحذير `⚠ لازم تقرّين بسياسة الاسترجاع المجاني قبل النشر`).
- **معاينة حية (`👁️ كذا يظهر إعلانك للعميلات`)** تتحدث مع كل تغيير (`lpSync`).
- أزرار `نشر الإعلان`/`حفظ كمسودة`. نجاح النشر: `✓ تم نشر إعلانك! منتجك الآن ظاهر في قسمه وصفحة متجرك — بنبلغك بأول طلب 🔔`. وضع التعديل: `✓ حُفظت تعديلاتك على «{العنوان}»!`.

### ٣.٥ السلة والدفع (`cartPage`)

- عناصر متعددة البائعات (لكل بائعة **شحنة وكود تسليم مستقل**). ملاحظة Escrow: `🔒 ملاحظة: طلبك من بائعتين مختلفتين — بيوصلك شحنتين، ولكل شحنة كود تسليم مستقل في «طلباتي».`
- عنصر: العنوان + `{البائعة} · شراء بتعديل المقاسات · اللون: وردي` + `+ إضافة: ذيل للفستان (١٥٠) · تغليف سبيشل (٤٥)` + `مقاساتك وملاحظاتك مرفقة ✓` + السعر + `حذف`.
- فارغة: `سلتك فاضية 🛍️` + `تصفحي تشكيلات المصممات ←`.
- **الكوبونات:** `🎟️ عندك كوبون؟` + input (placeholder `اكتبي الكود — جربي ANAQA10 أو REEM100`) + `تطبيق`. رسائل: غير صحيح `✕ الكود غير صحيح أو منتهي` · كوبون بائعة بلا منتجات منها `✕ هذا كوبون {البائعة} — ما عندك منتجات منها بالسلة` · دون الحد `✕ الكوبون يتطلب حدًا أدنى {min} ر.س` · نجاح `✓ {الوصف} — انخصم {d} ر.س` + شارة `كوبون أناقتك`/`كوبون بائعة`.
- ملخص: `المجموع الفرعي` + توصيل لكل بائعة + سطر خصم الكوبون `🎟️ خصم الكوبون ({الكود})` + `الإجمالي`.
- **طرق الدفع:** `مدى` (افتراضي) · `بطاقة ائتمانية` · `Apple Pay` · `تابي — ٤ دفعات` · `تمارا` · `محفظة أناقتك` (عبر تجريد PaymentGateway).
- زر `ادفعي {الإجمالي} ر.س` + note `🔒 مبلغك محجوز لدى أناقتك ولا يصل البائعات إلا بعد تأكيدك الاستلام بكود التسليم · الاسترجاع خلال ٧ أيام مجاني`.
- نجاح: `✓ تم الدفع وحجز المبلغ! رقم الطلب #A-1043 ...` + `💬 انفتحت محادثات البائعات لطلبك الآن — بما فيها بائعات «الدفع أولًا»` + `اذهبي لطلباتي ←`.

### ٣.٦ طلباتي (`ordersPage`)

خطوط زمنية بخطوات (`done`/`cur`/plain). أمثلة:
- `#A-1043` فستان سهرة (تعديل مقاسات) — ريم كوتور · `قيد التنفيذ` · خطوات: `مدفوع ومحجوز`→`قبلته المصممة`→`قيد التنفيذ (يوم ٤ من ١٢)`→`مع المندوب`→`تم التسليم`.
- **كود تسليم ٤ أرقام** بصندوق تحذير: `كود التسليم — لا تعطينه للمندوب أو البائعة إلا بعد استلام القطعة ومعاينتها` / `⚠ إعطاء الكود = تأكيد الاستلام وتحويل المبلغ للبائعة`.
- `#A-1043-B` (شحنة ثانية بكود مستقل).
- `#A-1027` تأجير مكتمل: `مكتمل — التأمين استُرد ✓`، `تأمين ٥٠٠ ر.س رجع كاملًا لمحفظتك`.
- أزرار `💬 محادثة المصممة` · `كل طلباتي وحالاتها ←`.

### ٣.٧ حسابي — البروفايل المزدوج (`buyerDash`)

- ترويسة `حسابي — أهلًا نوف 🌹` + `بروفايلك المزدوج: مشترياتك ومبيعاتك ومحفظتك الموحدة — كل الأرقام محسوبة من عملياتك الفعلية.`
- **المحفظة الموحدة (`💰 محفظتي الموحدة — كل حركة لها قصة`):** الرصيد = مجموع كشف الحركات. رقائق: `🔒 محجوز من مبيعات جارية` · `🛡️ تأمينات محجوزة لصالحك كبائعة` · `🛡️ تأميناتك المدفوعة المحجوزة كمشترية`. سحب (`سحب من الرصيد` — حد أدنى ١٠٠، ≤ الرصيد؛ خطأ `✕ المبلغ لازم يكون بين ١٠٠ ر.س ورصيدك الحالي ({bal} ر.س)`؛ نجاح يضيف سطر سحب سالب).
- **كروت إحصائية تفتح أقسامها عند الضغط (`bdScroll`/`bdGo`):** `رصيد محفظتي` · `طلبات نشطة` · `مشترياتي هذا الشهر` · `طلب مكتمل` · `تأمينات محجوزة حاليًا` · `تأمينات استُردت لي` · `خُصم من تأميناتي (تلف)` · `تقييمات بانتظارك ⭐`.
- **تبويبان (`profTab`):** `🛍️ مشترياتي` / `🏪 مبيعاتي ومتجري`.
- **الطلبات بفلاتر (`bdFilter`):** `الكل`/`النشطة`/`المكتملة`/`الملغاة`. hint الإلغاء المجاني. أزرار حسب الحالة: `تتبّع الطلب ←` · `إلغاء مجاني` · `↩️ طلب إرجاع مجاني (متبقي {X} أيام)` · للتفصيل المكتمل `الإبلاغ عن عيب مصنعي` (alert: `منتجات التفصيل الخاص تُسترجع فقط في حال عيب مصنعي...`) · `حذف من السجل` (يُمنع للنشط: `ما ينحذف طلب نشط — ألغيه أولًا إن كان قابلًا للإلغاء`).
- **جدول التأمينات:** أعمدة `القطعة · التأمين · الحالة · عمليات`. حالات: `محجوز — يرجع بعد الإرجاع السليم` · `خُصم {cut} ({السبب}) — استُرد {dep-cut}` · `✓ استُرد كاملًا`. أزرار `تتبّع`/`إلغاء واسترداد`/`التفاصيل`/**`اعتراض`** (`depAppeal`: `✓ سُجّل اعتراضك برقم #APL-{id}`).
- **منتجاتي (`renderMyProds`):** بطاقات + `＋ أضيفي منتج جديد` + تعديل/حذف (`حذف «{الاسم}» من المنصة نهائيًا؟ الطلبات القائمة عليه تستمر حتى تكتمل.`).
- **صندوق طلب ذكي مطابق:** `🪄 طلب ذكي مطابق لمتجرك` + input `سعر عرضك` + `قدمي عرضك` (رفض إذا > الميزانية).
- **وصول سريع:** `📦 أكواد التسليم` · `💬 محادثاتي` · `♥ مفضلتي` · `👜 سلتي` · `⭐ تقييماتي (٢)` · `📍 عناويني`.

### ٣.٨ لوحة المتجر (`sellerPage`)

`لوحة تحكم المصممة — دار نورة`. أقسام: إحصائيات (٦ كروت) · رسم مبيعات ٦ أشهر (خمري=بيع، ذهبي=تأجير) · `📦 المخزون` (يظهر للبائعة فقط) · `🧾 الطلبات وكود التسليم` (`confirmDelivery`: كود صحيح → تحرير، `net = amount×0.88`, عمولة `amount×0.12`) · **دورة القطعة المؤجرة** (`استلمتها سليمة ✓` → التأمين يرجع للعميلة والقطعة تتاح؛ `فيها تلف — فتح نزاع` → `⚠ فُتح نزاع تلف — ... خلال ٢٤ ساعة`) · `🏦 المحفظة وسحب الرصيد` (حد أدنى ١٠٠، `doWithdraw`) · `📞 التواصل` (الأوضاع الثلاثة) · `🏪 حالة المتجر` (مفتوح/إيقاف طلبات/مقفل) · `🎀 التغليف` · `🚚 التوصيل` · **`🎟️ كوبونات متجري`** (`addCpn`: كود `[A-Z0-9]{3,15}`، نسبة ≤٧٠٪، تفعيل/إيقاف/حذف؛ العمولة تُحسب بعد الخصم) · `💰 عمولة المنصة ١٢٪`.

### ٣.٩ الطلب الذكي (`smartReqPage`)

- `🪄 الطلب الذكي — البائعات يتنافسن عليك`.
- نموذج: `وصف طلبك *` (textarea) · `القسم *` · `المقاس` (S/M/L/XL/XXL/`حسب مقاساتي المحفوظة`) · `الميزانية القصوى (ر.س) *` · `أحتاجه قبل تاريخ` · `نوع الطلب` (شراء/تأجير/شراء أو تأجير/**`🎨 تصميم خاص — عندي صورة موديل`**) · `نطاق البائعات` (منطقتي أولًا ثم الجميع / منطقتي فقط / كل المناطق).
- **صندوق التصميم الخاص:** `🎨 ارفعي صور الموديل المرجعي` + خانات صور (`صورة الموديل *`، `زاوية أخرى`، `خامة تفضلينها`) + حقل `وش تبين يتغير عن الموديل الأصلي؟` + البند القانوني: `⚖️ الصورة تُستخدم كمرجع إلهام فقط — التنفيذ تصميم مستقل باسم المصممة المنفذة، ولا تُقبل علامات تجارية مقلدة (شعارات أو تواقيع دور أزياء).`
- نشر مجاني: `🪄 انشري طلبك للبائعات` + `مجاني تمامًا — وما يوصل رقمك لأي بائعة، التواصل كله عبر المنصة`.
- **العروض تصل حية (WebSocket)** ببطاقات: سعر · مؤشر ثقة البائعة (تقييم + التزام/صفقات موثقة) · شارة منطقتك · شارة (`الأنسب سعرًا`/`الأعلى ثقة`/`أوفر خيار`). hint `العروض الجديدة تستمر بالوصول حتى ٢٤ ساعة`.
- **قبول عرض** (`srAccept`): تأكيد `... سينشأ الطلب ويُحجز المبلغ لدى أناقتك (Escrow) ...` → إنشاء طلب Escrow فعلي (`#A-1052`).
- استفسار بالشات · `إغلاق الطلب — ما عاد أحتاجه`.

### ٣.١٠ الشات (`chatPage`)

- مرتبط بالطلب (`المحادثة مرتبطة بالطلب #A-1043 · محمية وموثّقة لدى أناقتك`).
- **عرض الإضافة الرسمي:** بطاقة `💎 عرض إضافة رسمي` + سعر + `موافقة — تنضاف للطلب` / `رفض`. القبول: `✓ وافقتِ على الإضافة — انضافت لسعر الطلب #A-1043`.
- ملاحظة نظامية: `عروض الإضافات تنضاف للطلب رسميًا داخل المنصة — لا تحوّلين أي مبلغ خارج أناقتك`.
- احترام وضع «الدفع أولًا» (الشات مقفل قبل الدفع، يُفتح بعده تلقائيًا).
- **فلتر منع التعامل الخارجي** (§٤.١٠): يمنع أرقام/روابط دفع خارجية + تحذير + إبلاغ أدمن. *(النموذج يذكر السياسة نصًّا؛ نُنفّذ الفلتر فعليًا في الباك إند.)*

### ٣.١١ المفضلة (`favsPage`)

`مفضلتي (٣)`. بطاقات قابلة للفتح. للمتاجر الموقوفة: `⏸ البائعة موقفة الطلبات — بنبلغك أول ما تفتح 🔔`.

### ٣.١٢ مركز الإشعارات (`notifPanel`)

`🔔 إشعاراتي` + `تحديد الكل كمقروء` + `✕`. عداد `nBadge` = غير المقروء. أنواع: عرض طلب ذكي، استرداد تأمين، شحنة مع المندوب، إعلان مميز ينتهي غدًا، اكتمال إرجاع. الضغط → تحديد كمقروء + تنقل عميق (`notifGo`).

### ٣.١٣ شاشة التقييم (`rateModal`)

`⭐ قيّمي تجربتك` — ٥ نجوم + شرائح (`جودة القماش ممتازة`/`مطابق للوصف`/`تغليف راقي`/`سرعة بالتجهيز`/`تعامل رائع`) + نص اختياري + `نشر التقييم`. hint `تقييمك يُحتسب بمؤشر ثقة البائعة تلقائيًا ولا يمكن شراؤه أو تعديله يدويًا`. حصري للطلبات المكتملة؛ عداد `تقييمات بانتظارك`. نشر: `✓ نُشر تقييمك ({n} ★) بشارة «شراء موثّق»...`.

### ٣.١٤ السياسات (`policyPage`)

`سياسات أناقتك` — `آخر تحديث: يوليو ٢٠٢٦`. **٧ أقسام** بنصوص الملف حرفيًا (تُنقل كما هي إلى الباك إند/التطبيق):
`↩️ سياسة الاسترجاع المجاني` · `👗 سياسة التأجير والتأمين` · `🔒 الشراء الآمن (Escrow) وكود التسليم` · `🚚 الشحن والتوصيل` · `💰 العمولة والرسوم` · `🔐 سياسة الخصوصية` · `📜 الشروط والأحكام`. تنقّل عميق عبر `openPolicy('return'|'rental'|'escrow'|'shipping'|'commission'|'privacy'|'terms')`.

### ٣.١٥ تسجيل OTP (`loginModal`)

خطوة الهاتف: `سجّلي دخولك` + `+966` + placeholder `5XXXXXXXX` (٩ أرقام) + **اختيار المنطقة** (`منطقتك 📍 — عشان نعرض لك القريب منك أولًا والإعلانات المميزة بمنطقتك`) — ١٢ منطقة + `أرسلي الكود` (تحقق: `أدخلي رقم جوال صحيح (٩ أرقام تبدأ بـ 5)`).
خطوة OTP: `أدخلي كود التحقق` + ٤ خانات (auto-advance) + خطأ `الكود غير صحيح — تأكدي من الرسالة وحاولي مرة ثانية` + نجاح `✓ تم التحقق — أهلًا فيك في أناقتك!` + `إعادة إرسال الكود`. كود التطوير `1111`. النجاح يضبط المنطقة ويكمل للوجهة المعلّقة.

### ٣.١٦ المودالات

- **الإرجاع (`retModal`):** `↩️ طلب إرجاع مجاني` + `وش سبب الإرجاع؟` (راديو: `المقاس غير مناسب` [افتراضي] / `المنتج يختلف عن الوصف أو الصور` / `غيّرت رأيي` / `عيب في المنتج`) + note مجاني + `تأكيد طلب الإرجاع`.
- **التقييم (`rateModal`):** أعلاه ٣.١٣.

**المناطق الـ١٢:** الرياض، جدة، مكة المكرمة، الدمام، الطائف، أبها، المدينة المنورة، بريدة، تبوك، حائل، جازان، نجران.

---

## ٤) قواعد العمل غير القابلة للتفاوض

1. **Escrow:** المبلغ يُحجز عند الدفع، لا يصل البائعة إلا بإدخالها **كود التسليم (٤ أرقام)** الذي تمنحه المشترية بعد المعاينة. العمولة **١٢٪** تُخصم عند التحرير (`net = amount × 0.88`)، وتُحسب على السعر **بعد كوبون البائعة** (كوبون المنصة على حساب المنصة ولا يمس مستحقات البائعة). طلب متعدد البائعات = شحنة وكود لكل بائعة.
2. **الاسترجاع:** ٧ أيام **مجانية بالكامل على المشترية** (شحن الإرجاع على البائعة). استثناء وحيد: التفصيل الخاص — عيب مصنعي فقط. البائعة تُقر بالسياسة عند كل نشر (checkbox إلزامي). المبلغ يعود للمحفظة فور تأكيد البائعة الاستلام. النافذة تُعرض كـ «متبقي X أيام».
3. **التأجير:** سعر/٣ أيام + تأمين منفصل محجوز لدى المنصة. تلف كامل = التأمين كاملًا للبائعة؛ جزئي = تقدير الأدمن خلال **٢٤ ساعة** من النزاع بصور التسليم والإرجاع؛ تأخير = **١٠٪/يوم**. القطعة تُحجب آليًا (إيجار + إرجاع + يوم فحص) ولا تتاح إلا بتأكيد «سليمة». إرجاعها مجاني على المشترية. ممنوع الغسيل الذاتي.
4. **المميز:** باقات مسبقة الدفع — **منطقتي:** ٣أيام/٢٩، ٧/٤٩، ١٥/٧٩ · **كل المناطق:** ٣/٥٩، ٧/٩٩، ١٥/١٤٩ ر.س. تُدفع عند النشر (محفظة/بطاقة). عدد أماكن الشريط محدود. انتهاء تلقائي + إشعار «ينتهي غدًا».
5. **المنطقة:** لكل مستخدم منطقة (تسجيل + محدد دائم). الترتيب: منطقته أولًا (شارة خضراء). المميز يُعرض حسب نطاق باقته.
6. **الكميات لا تظهر للمشتريات إطلاقًا** — نفادها يعطل الخيار فقط (اللون/المنتج)، وتنبيه انخفاض مخزون للبائعة (`١ — نبهيني للتزويد ⚠`، `٠ — مخفي تلقائيًا`).
7. **الطلب الذكي:** عرض البائعة > الميزانية = **مرفوض** (`عرضك أعلى من ميزانية الطلب — ما راح يظهر للعميلة`). صلاحية العروض ٢٤ ساعة (قابلة للتمديد). حد **٣ طلبات نشطة** لكل مستخدمة. القبول = إنشاء طلب Escrow.
8. **التواصل:** الأوضاع الثلاثة تُحفظ بالمتجر وتؤكَّد بكل إعلان؛ «الدفع أولًا» يقفل الشات قبل الدفع ويفتحه آليًا بعده مع تنبيه واضح للمشترية قبل الدفع؛ إظهار الرقم للموثقات فقط.
9. **التقييم:** حصري للطلبات المكتملة (شراء/تأجير موثّق)، يُحتسب في مؤشر ثقة البائعة آليًا ولا يُعدّل يدويًا.
10. **منع التعامل الخارجي:** فلتر أرقام/روابط دفع خارجية في الشات + تحذير + إبلاغ أدمن.

### ثوابت عددية (من JS)

| القاعدة | القيمة |
|---|---|
| العمولة | ١٢٪ (`net=×0.88`, `commission=×0.12`) |
| مصفوفة المميز | `{region:{3:29,7:49,15:79}, all:{3:59,7:99,15:149}}` |
| مدد المميز | ٣، ٧، ١٥ يوم (افتراضي region/7) |
| صيغة كود الكوبون | `[A-Z0-9]{3,15}` |
| أقصى نسبة كوبون | ٧٠٪ |
| الحد الأدنى للسحب | ١٠٠ ر.س |
| رفض عرض الطلب الذكي | إذا > الميزانية |
| حد الطلبات النشطة | ٣ |
| شحن مجاني للطلب | فوق ٣٠٠ ر.س |
| تأخير التأجير | ١٠٪/يوم |
| تقدير التلف الجزئي | خلال ٢٤ ساعة |
| مراجعة الاعتراض | ٢٤–٧٢ ساعة |
| كود التطوير (OTP + التسليم) | `1111` |
| طول الهاتف | ٩ أرقام تبدأ بـ 5 |
| توصيل الفروع الافتراضي | داخل المدينة ١٥، مناطق أخرى ٣٠ |
| فترة الحجب التجريبية | 2026-07-08 → 2026-07-13 (تتاح ١٤/٧) |

---

## ٥) قاعدة البيانات

الجداول (الحد الأدنى — تُكمَّل باحتراف بمفاتيح أجنبية وفهارس):

```
users(id, name, phone, region_id, is_verified_seller, fcm_token, ...)
regions(id, name)                          -- 12 منطقة seed
stores(id, user_id, name, slug, bio, status[open|paused|closed],
       comm_mode[chat|call|payfirst], phone_visible, packaging(json),
       delivery_free_km, delivery_flat_price, verification_status, joined_year)
products(id, store_id, category, title, description, condition[new|used_*],
         code, region_id, status[live|out|draft], is_single_piece)
product_modes(id, product_id, type[ready|custom|rent], price DECIMAL(10,2),
              deposit DECIMAL(10,2) nullable, prep_time, exec_time, rent_scope)
product_colors(id, product_id, name, hex, qty)          -- qty مخفي عن المشتريات
product_addons(id, product_id, name, price DECIMAL(10,2))
listings_featured(id, product_id, scope[region|all], days[3|7|15],
                  price DECIMAL(10,2), starts_at, expires_at, status[active|expired])
orders(id, code, buyer_id, parent_order_id nullable, status, total DECIMAL,
       is_custom, region_id, placed_at, cancellable)
order_items(id, order_id, product_id, store_id, mode, color, size_profile_id,
            addons(json), packaging, unit_price, delivery_code(4), delivery_fee)
escrow_transactions(id, order_id, store_id, held_amount, commission,
                    net_amount, status[held|released|refunded], released_at)
wallets(id, user_id, balance DECIMAL(10,2))
wallet_ledger(id, wallet_id, type, ref, text, amount DECIMAL(10,2), created_at)
deposits(id, order_id, buyer_id, store_id, amount DECIMAL(10,2),
         status[held|refunded|partially_cut|fully_cut|appealed],
         cut_amount, cut_reason)
rental_bookings(id, product_id, order_id, occasion_date, block_from, block_to,
                inspection_day, status[booked|out|returning|inspection|ok|dispute])
returns(id, order_id, reason, status, timeline(json), days_left)
disputes(id, type, order_id, evidence(json), decision, decided_by, cut_value)
coupons(id, owner[platform|store], store_id nullable, code, kind[fix|pct],
        value DECIMAL, cap DECIMAL nullable, min DECIMAL, used_count,
        active bool)
coupon_redemptions(id, coupon_id, order_id, user_id, discount DECIMAL)
smart_requests(id, buyer_id, type[buy|rent|both|custom], description, category,
               size, budget DECIMAL, need_by, scope, ref_images(json),
               change_notes, status[open|matched|accepted|expired|closed],
               expires_at)
smart_request_offers(id, request_id, store_id, price DECIMAL, message,
                     status[sent|accepted|rejected|hidden])
conversations(id, order_id nullable, buyer_id, store_id, locked bool)
messages(id, conversation_id, sender_id, type[text|addon_offer|system], body,
         created_at)
addon_offers(id, message_id, order_id, name, price DECIMAL,
             status[pending|accepted|rejected])
ratings(id, order_id, store_id, buyer_id, stars, chips(json), text,
        verified_type[purchase|rental])
notifications(id, user_id, ic, title, body, go, read_at)
addresses(id, user_id, region_id, label, is_default)
otp_codes(id, phone, code, expires_at, consumed_at)
size_profiles(id, user_id, shoulder, chest, waist, hip, upper_arm,
              sleeve_length, full_length, unit[cm|inch], photo)
appeal_tickets(id, deposit_id, ref[APL-*], status, reviewed_by)
```

Seeders تُعيد إنتاج **كل بيانات النموذج حرفيًا** (المصممات، المنتجات، الطلبات A-0940…A-1052، الكوبونات، كشف المحفظة، الإشعارات، العروض) — §٧ أدناه.

---

## ٦) آلات الحالة (Enums + Transitions محمية + أحداث)

- **Order:** `pending_payment → paid_escrow → accepted → in_progress → with_courier → delivered(code) → completed` + `cancelled(free قبل accepted)` + `return_requested → return_pickup → returned_refunded`.
  - تعيينات النموذج: `wait`(بانتظار قبول/تجهيز) · `run`(قيد التنفيذ/مع المندوب) · `done`(مكتمل) · `return` · `cancel`.
- **Deposit:** `held → refunded | partially_cut | fully_cut` (+ `appealed`).
- **Rental:** `booked → out → returning → inspection → ok | dispute`.
- **Featured:** `active → expired` (Scheduler يومي + إشعار قبل ٢٤ ساعة).
- **SmartRequest:** `open → matched → accepted | expired | closed`.

كل انتقال مالي يكتب سطرًا في `wallet_ledger` أو سجل تدقيق. أمثلة نصوص الحركات (حرفية):
`💰 دخل بيع — طلب #A-1039 عباية مطرزة (بعد عمولة ١٢٪)` · `استرداد تأمين كامل — عباية زفّة #A-1027` · `⬇ سحب لحسابك البنكي — مصرف الراجحي` · `↩️ استرداد إرجاع — {العنوان} #{id}`.

---

## ٧) البيانات التجريبية (Seed حرفي)

### المصممات/المتاجر
| المتجر | التخصص | التقييم | طلبات | انضمام | الحالة |
|---|---|---|---|---|---|
| دار نورة للعبايات | عبايات مناسبات وتفصيل | ٤.٩ | +٦٢٠ | ٢٠٢٣ | مفتوح · التزام ٩٧٪ |
| ريم كوتور | فساتين سهرة — بيع وتأجير | ٤.٨ | +٤١٠ | ٢٠٢٤ | مفتوح · comm=call |
| ورد الجوري للعناية | خلطات طبيعية وكريمات | ٤.٩ | +٨٩٠ | ٢٠٢٢ | مفتوح |
| جواهر هند | إكسسوارات ومشغولات يدوية | ٤.٧ | +٢٧٠ | ٢٠٢٤ | مفتوح |
| لمسة لطيفة | فساتين أطفال ومواليد | ٤.٨ | +١٥٠ | ٢٠٢٥ | مفتوح |
| خزانة سارة | فساتين مستعملة — بيع وتأجير | ٤.٦ | +٩٥ | ٢٠٢٥ | **موقوف** · comm=payfirst |
| عطر وعود | خلطات عطرية وعناية بالشعر | ٤.٩ | +٣٤٠ | ٢٠٢٤ | مفتوح |
| مخزون منيرة | عبايات بدون براند — دفعات | ٤.٥ | +٢١٠ | ٢٠٢٤ | **مقفل** |
| خزانة العنود | مستعمل موثّق | ٤.٨ | — | — | — |
| مخزون مصممات | بدون براند | ٤.٥ | — | — | — |

### المنتجات (`PRODUCTS`)
| id | العنوان | المتجر | الكود | الأنماط (سعر/تأمين) | flags |
|---|---|---|---|---|---|
| dress1 | فستان سهرة ساتان بقصّة ملكية | ريم كوتور | A2605 · مجموعة عيد ٢٠٢٦ | ready 1250 · custom 1400 · rent 180/300 | comm=call, هاتف 0551234567, ألوان وردي/أسود/ذهبي(نفد) |
| abaya1 | عباية مناسبات مطرزة بخيوط ذهبية | دار نورة | N-114 | ready 950 · custom 1100 · rent 140/250 | comm=chat |
| rent1 | عباية زفّة مطرزة بالكامل — قطعة واحدة | دار نورة | N-201 | rent 320/500 | قطعة واحدة · مؤجرة حتى ١٤/٧ |
| noura3 | عباية كريب يابانية بقصّة كلوش | دار نورة | N-087 | ready 540 | — |
| noura4 | فصّلي عبايتك على مقاسك | دار نورة | N-300 | custom 750 | — |
| care1 | كريم الورد الطائفي للبشرة | ورد الجوري | W-12 · ٥٠ مل | ready 85 | — |
| acc1 | طقم إكسسوارات بحجر العقيق | جواهر هند | J-55 | ready 220 | — |
| used1 | فستان زفاف لُبس مرة واحدة | خزانة سارة | S-09 | ready 1800 · rent 250/400 | مستعمل ممتاز · comm=payfirst · موقوف |
| nobrand1 | عبايات كريب أسود — دفعة ٥ قطع | مخزون مصممات | B-33 | ready 390 | بدون براند |
| used2 | حقيبة سهرة مرصّعة | خزانة العنود | E-21 | ready 310 | مستعمل · **مباعة** |

**إضافات المنتجات:** dress1 (تطريز أكمام ٨٠ / بطانة كاملة ١٢٠ / شيلة بنفس القماش ٢٠٠ / ذيل ١٥٠) · abaya1 (تطريز ياقة ٦٠ / أزرار ذهبية ٤٠) · noura4 (تطريز أكمام ١٥٠ / بطانة حرير ١٨٠) · acc1 (نقش اسم ٣٥ / علبة مخمل ٢٥).
**التغليف:** عادي ٠ / بسيط ١٥ / سبيشل ٤٥ / هدايا ٦٥.

### الطلبات (`BD.orders`)
`A-1051` (إكسسوارات، جواهر هند، ٢٤٥، wait) · `A-1050` (تأجير، ريم كوتور، ١٨٠+تأمين٣٠٠، wait) · `A-1043` (فستان تعديل، ريم كوتور، ١٧٠٥، run، يوم ٤/١٢، غير قابل للإلغاء) · `A-1043-B` (كريم الورد، ورد الجوري، ١١٠، run) · `A-1042` (فستان وردي، ١٣١٠، نوف م.، محجوز) · `A-1040` (تأجير عباية زفّة، عائدة) · `A-1038` (حقيبة، خزانة العنود، ٣١٠، done، إرجاع متبقي ٤ أيام حتى ٩ يوليو) · `A-1027` (تأجير، دار نورة، ٣٢٠+٥٠٠، done، تأمين مسترد) · `A-0991` (تأجير، خزانة سارة، ٢٥٠+٤٠٠، done، مسترد) · `A-0962` (تأجير، دار نورة، ٢٠٠+٢٥٠، done، **خصم ٥٠ بقعة**) · `A-0940` (شال، جواهر هند، ١٦٠، **ملغي**).

### الكوبونات
- المنصة: `ANAQA10` (pct 10، cap 50، min 100).
- بائعات: `REEM100` (ريم كوتور، fix 100، min 1000) · `NOURA50` (دار نورة، fix 50، min 500) · `EID15` (pct 15، min 400، **موقوف**، used 41) · `NOURA50` used 14.

### كشف المحفظة (نوف — الأحدث أولًا)
`+1152.8` دخل بيع #A-1039 · `+500` استرداد تأمين #A-1027 · `+281.6` دخل تأجير #A-1031 · `+200` استرداد جزئي #A-0962 · `+160` استرداد إلغاء #A-0940 · `−1500` سحب الراجحي · `−360` دفع جزء من #A-1043-B. **الرصيد = ٤٣٤٫٤ ر.س**.

### الإشعارات / العروض
- إشعارات: عرض طلب ذكي (دار نورة ٧٩٠) · استرداد تأمين ٥٠٠ · شحنة مع المندوب · مميز ينتهي غدًا (غير مقروء) · إرجاع اكتمل ٣١٠ (غير مقروء).
- عروض طلب ذكي (ميزانية ٩٠٠): دار نورة ٧٩٠ (الأنسب سعرًا، منطقتك) · ريم كوتور ٨٥٠ (الأعلى ثقة) · خزانة سارة ٥٢٠ (أوفر خيار، منطقتك).

### إحصائيات لوحة المتجر (دار نورة)
رصيد قابل للسحب ٤٬٦٢٠ · مبيعات الشهر ١٢٬٤٥٠ (▲١٨٪) · صافي بعد العمولة ١٠٬٩٥٦ · ٣٤ طلب (٢٨ بيع · ٦ تأجير) · تأمينات محجوزة ٥٥٠ · تقييم ٤.٩ · محجوز للطلبات الجارية ١٬٨٤٠ · IBAN `SA•• ••41 2035 — مصرف الراجحي`.

---

## ٨) لوحة الأدمن (Filament 3)

- **توثيق البائعات:** مستندات (معروف/سجل تجاري) · قبول/رفض.
- **النزاعات:** عرض صور الطرفين (تسليم/إرجاع) · قرار خصم التأمين بنسبة أو مبلغ · تنفيذ تلقائي على المحافظ.
- **الاعتراضات (Appeals):** مراجعة `APL-*` والرد ٢٤–٧٢ ساعة.
- **الطلبات والمبالغ المحجوزة (Escrow).**
- **السحوبات:** قائمة تحويلات معلقة + تصدير.
- **كوبونات المنصة · إدارة المميز · المستخدمون (إيقاف/حظر) · بلاغات الشات.**
- **إحصائيات:** GMV · العمولات · إيراد المميز.

---

## ٩) الجودة والاختبارات (بوابة إقفال كل مرحلة)

- **Backend Pest** لكل قاعدة §٤، خصوصًا: العمولة بعد الكوبون · حجب التأجير · نافذة الإرجاع · رفض عرض>الميزانية · قفل شات الدفع-أولًا. `phpstan` مستوى ٦+ و`pint` نظيفان.
- **Flutter:** `flutter analyze` صفر تحذيرات · اختبارات Widget (منتج/سلة/محفظة) · Golden للـRTL.
- **أمن:** Policies لكل Endpoint · Rate limiting (OTP، الطلب الذكي) · تحقق خادمي لكل الأسعار (لا ثقة بمبالغ العميل) · فحص نوع/حجم الصور.
- **مراجعة ذاتية مكتوبة** في نهاية كل مرحلة (نفّذت/اختبرت/الفجوات) ثم إصلاح الفجوات قبل المتابعة.

---

## ١٠) خطة التنفيذ

- **م٠ (الحالية):** SPEC.md + DECISIONS.md → انتظار التأكيد.
- **م١:** Laravel (Migrations/Models/Seeders + آلات الحالة + الخدمات + REST API موثّق + Pest).
- **م٢:** Filament Admin.
- **م٣:** Flutter (Design System + ١٦ شاشة بربط API حقيقي + WebSockets).
- **م٤:** تكامل E2E (السيناريوهات الخمسة).
- **م٥:** نشر (أيقونة/Splash + release + README/DEPLOYMENT/.env.example).

**قواعد عامة:** لا ميزات خارج النطاق · أي غموض → الأقرب لسلوك HTML ثم توثيقه في `DECISIONS.md` · النصوص العربية حرفية · السؤال فقط في المُعطِّل.
